name: Radar_Backup_System

on:
  schedule:
    - cron: '3,18,33,48 * * * *'  # 與主系統錯開3分鐘
  workflow_dispatch:

jobs:
  backup-radar:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
      - name: 備份系統檢查
        run: |
          echo "🔄 備份雷達系統檢查中..."
          sleep 30
          echo "✅ 備份系統正常"
