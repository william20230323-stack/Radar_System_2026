name: Triple_Radar_Full_Power

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  radar-mission:
    runs-on: ubuntu-latest
    steps:
      - name: 1. 檢出代碼
        uses: actions/checkout@v3

      - name: 2. 初始化 Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 3. 安裝組件
        run: pip install websocket-client requests

      - name: 4. 啟動三重雷達系統
        env:
          TG_TOKEN: ${{ secrets.TG_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
          TRADE_SYMBOL: ${{ secrets.TRADE_SYMBOL }}
        run: |
          # 啟動 V1/V2 實時監控 (背景模式)
          echo "📡 [武器庫 A-F] V1/V2 實時雷達點火..."
          python radar_commander.py &
          
          sleep 5
          
          # 啟動 V3 聰明錢戰報並守住進度條 (前景模式)
          echo "📡 [武器庫 A-F] V3 聰明錢戰報發射..."
          python smart_money_radar.py
