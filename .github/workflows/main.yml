      - name: 啟動三重防禦雷達系統
        env:
          TG_TOKEN: ${{ secrets.TG_TOKEN }}
          TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
          TRADE_SYMBOL: ${{ secrets.TRADE_SYMBOL }}
        run: |
          python radar_commander.py &  # 啟動 V1/V2 雷達並在背景運行
          sleep 5                      # 等待 5 秒確保連線穩定
          python smart_money_radar.py  # 啟動 V3 聰明錢主控核心
